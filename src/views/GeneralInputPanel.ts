import * as Vue from "vue";
import {Component} from "av-ts";

@Component({
  name: 'general-input-panel'
})
export default class GeneralInputPanel extends Vue {
  
  id : string = "GeneralInputPanel";
}